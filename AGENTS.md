# AGENTS.md

## 项目规则

- 最终总结文字要说重点，语句简明，不要长篇大论。
- 前端或桌面 UI 相关任务必须先读取根目录的 `DESIGN.md`。
- 本项目采用 `awesome-design-md` 中 `linear.app` 风格作为基础，并已在 `DESIGN.md` 中适配为 Windows 专用透明度调节工具的界面规范。
- 实现 UI 时优先遵循 `DESIGN.md` 的颜色、字体、间距、组件状态、布局和交互规则。
- 保留项目既有功能和技术栈；视觉规范与现有实现冲突时，先保持功能正确，再做最小范围的样式适配。
